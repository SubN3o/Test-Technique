{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="offset-4 col-4">
            <div class="form-group formulaireContact">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}